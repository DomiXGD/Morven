FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production
COPY package*.json ./
RUN npm ci --omit=dev
COPY server ./server
COPY tsconfig.json ./tsconfig.json
EXPOSE 4001
CMD ["node", "--import", "tsx", "server/index.ts"]
